# ============================================================================
# VARIÁVEIS DE AMBIENTE - GainYourMuscle Backend
# ============================================================================
# Copie este arquivo para .env e preencha com seus valores reais

# Porta do servidor
PORT=5000

# URL do Frontend (para CORS)
FRONTEND_URL=http://localhost:3000

# MongoDB - String de Conexão
# Opção 1: MongoDB Local
# MONGO_URI=mongodb://localhost:27017/gainyourmuscle

# Opção 2: MongoDB Atlas (Cloud - Recomendado)
# Substitua <username>, <password> e <cluster-url> pelos seus dados
# MONGO_URI=mongodb+srv://<username>:<password>@<cluster-url>/gainyourmuscle?retryWrites=true&w=majority

MONGO_URI=mongodb://localhost:27017/gainyourmuscle

# JWT - Token de Autenticação
# Gere uma string aleatória segura (mínimo 32 caracteres)
# Você pode gerar uma com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=sua_chave_secreta_super_segura_aqui_mude_isso

# Expiração do token (7 dias)
JWT_EXPIRE=7d

# Ambiente (development ou production)
NODE_ENV=development

# Groq API — IA para geração de treinos (gratuito em console.groq.com)
GROQ_API_KEY=sua_groq_api_key_aqui
